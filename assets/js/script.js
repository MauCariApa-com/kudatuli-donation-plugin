document.addEventListener('DOMContentLoaded', function() {
    // Check if the form should be reset
    var donationForm = document.getElementById('donation-form');
    if (donationForm) {
        donationForm.reset(); // Reset the donation form
    }
});